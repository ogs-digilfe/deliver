# AGENTS.md

## プロジェクト概要

`deliver` は、スクレイパーが取得した株式関連データをアップロードし、分析ホストがそのデータをダウンロードするための FastAPI サーバーである。Parquet、ZIP、HTML などのデータファイルをローカルファイルシステムへ保存・配信する。

通常運用では systemd から Uvicorn を起動し、TCP ポート 8080 で待ち受ける。現在の構成、API、制約の詳細は `README.md` と `docs/architecture.md` を参照すること。

## 作業開始時に確認するもの

変更前に、少なくとも次を確認すること。

- `README.md`: セットアップ、起動、systemd 運用
- `docs/architecture.md`: コンポーネント、API、状態、既知の制約
- `pyproject.toml` と `uv.lock`: Python バージョンと依存関係
- 変更対象のコード、および関連する呼び出し元
- `git status`: ユーザーの未コミット変更を上書きしないため

Python コードの作成、変更、レビュー、テスト、依存関係管理を行う場合は、グローバル指示で指定された `~/.codex/instructions/python.md` も最初に最後まで読むこと。

## 現在の構成

```text
.
├── deliver/
│   ├── main.py             # FastAPI アプリ、認証・認可、ファイル API
│   ├── lib_deliver.py      # ユーザー DB の読み込み、パスワード照合
│   ├── add_newadmin.py     # 管理ユーザー追加 CLI
│   ├── data/               # 配信データ（実行時生成、Git 管理外）
│   └── tmp/                # 一時領域（実行時生成、Git 管理外）
├── deploy/
│   └── deliver.service.example
├── docs/
│   └── architecture.md
├── userdb/
│   └── users               # 認証情報を含む JSON（Git 管理外）
├── pyproject.toml
└── uv.lock
```

既存構成を推奨レイアウトへ合わせることだけを目的に移動・再編しないこと。構成変更が必要な場合は、import、起動コマンド、systemd、ドキュメントへの影響を説明すること。

## 開発・実行コマンド

Python 3.12 以上と `uv` を使用する。仮想環境を手動で有効化せず、原則としてプロジェクトルートから `uv run` 経由で実行する。

```bash
# 開発用依存関係を含めて同期
uv sync

# 本番用依存関係のみ同期
uv sync --no-dev

# 開発サーバー
uv run uvicorn deliver.main:app --reload --host 0.0.0.0 --port 8080

# 管理ユーザー追加
uv run python -m deliver.add_newadmin

# JupyterLab
uv run jupyter lab
```

依存関係は `uv add`、`uv add --dev`、`uv remove` で変更し、`pyproject.toml` と `uv.lock` を同時に整合させること。標準ライブラリまたは既存依存関係で十分な場合は新規依存を追加しないこと。

現在、pytest、Ruff、Pyright はプロジェクト依存関係・設定に含まれていない。品質確認だけを目的に無断で追加しないこと。導入を提案する場合は、目的、設定、運用コマンド、既存コードへの影響を示すこと。

## 実装方針

- 既存の公開 API、URL、HTTP メソッド、認証要件、レスポンス形式、保存先、ファイル名を互換性のある契約として扱う。
- API 契約または保存形式を変更するときは、スクレイパーと分析ホストの両方への影響、移行方法、後方互換性を確認する。
- FastAPI の依存性注入を使った認証・認可の流れを維持し、アップロードは管理者、ダウンロードは active ユーザーという現行の権限境界を意図なく弱めない。
- ファイル操作には `pathlib.Path` を優先し、クライアント由来のファイル名は保存先ディレクトリ外へ脱出できないよう境界で検証する。絶対パス、`..`、シンボリックリンクを含むパストラバーサルを考慮する。
- 大容量ファイルを扱う可能性があるため、変更時は全内容のメモリ読み込み、応答のストリーミング、ディスク容量、一時ファイル、処理中断時の不完全ファイルを考慮する。
- アップロード中のファイルを分析ホストが取得しないよう、必要に応じて一時ファイルへ書き込み後に同一ファイルシステム内で原子的に置換する設計を検討する。
- Parquet などのバイナリデータをログやレスポンス本文へ不用意に出力しない。
- 公開関数と API 境界には型ヒントを付け、例外を握りつぶさず、クライアント向けエラーとサーバーログを区別する。
- 要求範囲外の大規模リファクタリングや、将来用途だけを理由にした抽象化を行わない。

## データ・認証情報の取り扱い

次の実行時データは機密情報または大容量データを含む可能性があるため、Git に追加しないこと。

- `deliver/data/`
- `deliver/tmp/`
- `userdb/users`
- `.env` および秘密値を含む設定ファイル
- Notebook の機密情報・大容量出力

実データや実ユーザー情報をテスト fixture、ログ、ドキュメント、サンプルへコピーしないこと。テストでは一時ディレクトリと架空の最小データを使うこと。パスワード、Bearer トークン、ソルト、ハッシュをログへ出さないこと。

## 現行実装で注意する制約

- ユーザー DB は `userdb/users` の JSON ファイルであり、プロセス起動時に一度だけ読み込まれる。
- 管理ユーザー追加後、実行中サーバーへの反映には再起動が必要である。
- Bearer トークンはプロセスメモリにあり、再起動で消失し、有効期限はない。
- 複数 Uvicorn ワーカー間でユーザー DB の更新やトークンは共有されない。
- パスワード保存はソルト付き SHA-256 であり、パスワード専用 KDF より総当たり攻撃への耐性が低い。
- 配信データはローカルディスクにあるため、複数サーバー間では共有されない。
- HTTPS、リバースプロキシ、レート制限、監査ログはアプリケーションでは提供していない。

これらは既知の改善候補だが、個別タスクで無断にまとめて変更しないこと。認証方式、トークン永続化、共有ストレージ、データベース化などの変更は運用・互換性への影響が大きいため、設計案と移行方針を提示して合意を得てから実施すること。

## テストと確認

変更内容に比例した確認を行うこと。テスト基盤を追加する場合は原則として pytest を使い、テストは `tests/` に配置する。

優先して確認する項目は次のとおり。

- `/token` の認証成功・失敗、inactive ユーザー、権限不足
- 各 upload/download API の正常系、ファイル不存在、空ファイル
- 不正なファイル名とパストラバーサル
- 保存先ディレクトリが存在しない場合の動作
- Parquet や ZIP などバイナリファイルの内容が往復後も一致すること
- 同名ファイル、同時アクセス、中断されたアップロードの挙動
- API 変更時の既存クライアントとの互換性

少なくとも変更した Python モジュールの構文確認を行い、利用可能な既存テストがあれば実行すること。サーバーを起動する確認では、ポート 8080 の競合や実データへの書き込みを避けること。テストや確認を実行できない場合は、その理由と未確認範囲を完了報告に明記すること。

## ドキュメントと運用設定

- セットアップ、コマンド、依存関係、API、保存先、認証、運用方法を変更した場合は `README.md` と `docs/architecture.md` を同じ変更で更新する。
- systemd の起動方法を変更した場合は `deploy/deliver.service.example` も更新する。
- systemd の例では `uv run --frozen --no-dev` を維持し、起動時にロックファイルや開発依存関係を変更しない。
- サービス実行ユーザーが必要最小限のファイル権限だけを持つようにする。
- 実サーバーの IP アドレス、ユーザー名、絶対パス、認証情報はリポジトリへ記録せず、例示にはプレースホルダーを使う。

## 完了時の報告

完了報告には、変更した内容、設計判断と理由、実行した確認、残る制約または未確認事項を簡潔に含めること。依存関係、公開 API、データ形式、運用手順に影響がある場合は、その影響範囲も明記すること。
