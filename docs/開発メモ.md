# 開発メモ

どこまでやったかのリマインダメモ

## 2026816

fast apiに、upload_portfolio_fileメソッドを追加した。
uploadテストにも成功。
ここでいったん、commitして、続きはwequantかportfolio-managerからuploadしたファイルをダウンロードするところから。

deliverはファイルの配布に徹し、zipファイルを保存するだけ。

portfolio-managerからダウンロードされたら、downloaded/とかに移す。
そのうち、不要なら消す。


zipの解凍やdbへのinsertなどはすべてクライアント側に任せる。

