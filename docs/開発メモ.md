# 開発メモ

どこまでやったかのリマインダメモ

## 2026816

fast apiに、upload_portfolio_fileメソッドを追加した。
uploadテストにも成功。
ここでいったん、commitして、続きはwequantかportfolio-managerからuploadしたファイルをダウンロードするところから。

deliverはファイルの配布に徹し、zipファイルを保存するだけ。

portfolio-managerからダウンロードされたら、downloaded/とかに移す。
そのうち、不要なら消す。


zipの解凍やdbへのinsertなどはすべてクライアント側に任せる。

- sbiとrakutenのportfolioをscrapeしてdeliverにuploadするところまで完成した(windowsから)
- deliver prodの容量が足りない。増やす必要がある。
- shikihoやkabutanのscrapingデータは非常に大きい。
- ここができたら、devでmergeしたmainを取り込んで、手元のsbi/rakutenのportfolioをdeliver prodにuploadする。
- その後、portfolio managerからdownloadしてdbに取り込んでいけるようにする。
- stockdbに入れ込むことも考えてみる。データが分散するとややこしいので。

